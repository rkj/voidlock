import { describe, it } from "vitest";

describe("CampaignManager (Renderer Bridge)", () => {
  it("is covered by engine tests", () => {
    // This file is now just a bridge and tests are handled in src/engine/managers/CampaignManager.test.ts
  });
});
