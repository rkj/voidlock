# src/engine/tests/generators

Tests for procedural map generation. Includes golden/snapshot tests for `DenseShipGenerator`, `SpaceshipGenerator`, and `TreeShipGenerator` to catch unexpected changes in generation output.
