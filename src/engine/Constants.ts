/**
 * Global engine constants for simulation scaling and normalization.
 */
export const SPEED_NORMALIZATION_CONST = 30;

/**
 * Standard unit physical radius in grid cells.
 */
export const UNIT_RADIUS = 0.3;
